<!DOCTYPE html>
<html lang="en">

<head>
  <link rel="stylesheet" href="/style.css" />
  <title><%= item.item %></title>
</head>

<body>
  <%- include("partials/header.ejs") %>
  <h2><%= item.item %></h2>
  <p>Description: <%= item.description %></p>
  <p>Manufacturer: <%= manufacturer.manufacturer %></p>
  <p>Categories: <% categories.forEach(category=>{ %>
    <a href="/categories/<%= category.id %>"><%= category.category %></a>
    <% }) %>
  </p>
  <p>Price: <%= item.price %></p>
  <p>Quantity In Stock: <%= item.quantity %></p>
  <a href="/items/<%= item.id %>/update">Edit</a>
  <form action="/items/<%= item.id %>/delete" method="POST" style="display: inline;">
    <button type="submit" onclick="return confirm('Are you sure you want to delete this item?')">
      Delete
    </button>
  </form>
</body>

</html>