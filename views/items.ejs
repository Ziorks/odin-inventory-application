<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= title %></title>
  </head>
  <body>
    <h1><%= title %></h1>
    <% if(locals.id) { %>
    <h2><%= id %></h2>
    <% } else { %>
    <h2>Items List</h2>
    <% } %>
  </body>
</html>
